# 项目研发规范与协同宪章 (RULES.md)

本文档严格依据通用系统架构设计艺术与工业级规范指南 (Modular Project Architect) 制定，既是 `mobile_robot_station` 项目全生命周期软件工程设计、模块解耦与代码实现的最高法典，也是开发者与 AI 助手在该项目中协同开发时必须无条件严格遵守的行为铁律。

---

## 一、核心行为与人机协同铁律 (Human-in-the-Loop)

### 1. 【开发者终极决策权与严禁无头模式验证 (Strict Ban on Headless Mode)】
- **开发者是系统的终极决策者**：AI 助手是高能执行伙伴，但严禁越俎代庖代替开发者做最终主观断定。AI 严禁以“后台断言通过”为由剥夺开发者直观研判系统物理状态与交互行为的权利；
- **严禁可视任务无头模式盲跑**：凡涉及 **2D 图像视觉、3D 物理空间仿真、底盘运动与姿态动力学、自主导航避障轨迹、激光雷达点云建图、双机位推流视讯、Qt 6 工业控制台** 等任何具备空间、几何或动态交互特性的功能验证，**严禁私自开启无头/离屏静默模式（如 `--headless`、`offscreen=True`、纯后台无窗口运行）**；
- **强制拉起真实可视化视窗**：
  - 必须拉起真实可视化视窗（MuJoCo 原生 3D 渲染视口、Qt 6 控制台界面或 GStreamer 视讯拉流窗口），将系统运动过程真实暴露在屏幕上；
  - **核心目的**：移动底盘侧翻倾覆、轮系悬空打滑、奇异点剧烈抽搐、几何模型穿透碰撞、雷达视线盲区切线擦碰、视讯色彩颠倒等致命物理缺陷，常规纯代码单元测试断言往往无法察觉甚至误判为“正常”，人类开发者凭借视觉直觉可在数秒内精准发现核心隐患并迅速决策。

### 2. 【严禁使用 Emoji (Strictly Forbid Emojis)】
- 在编写或更新代码注释、README.md、RULES.md、开发文档、提交信息 (Git Commit) 及所有对话回复中，**严禁使用任何 Emoji 表情符号**；
- 全程保持工业级严肃、严谨、克制与专业的技术工程风格，杜绝情绪化修辞。

### 3. 【零绝对路径与无宿主绑定 (Zero Host Coupling)】
- **严禁硬编码任何绝对路径**：严禁在代码、文档（README.md）、配置文件或示例命令中出现宿主机私有路径（如包含个人用户名或驱动器盘符的绝对路径）；
- **动态相对寻址原则**：代码中加载任何模型资产（XML/STL）、配置文件（YAML）、数据集（PLY）或脚本，必须基于调用文件自身的相对位置动态计算（如 Python 的 `os.path.join(os.path.dirname(__file__), ...)` 或 `Path(__file__).resolve().parent`，CMake 的 `${CMAKE_CURRENT_SOURCE_DIR}`）；
- **文档链接通用化**：Markdown 文档中的跳转链接必须使用仓库内通用相对路径（如 `[驱动模块](simulation/robot_driver.py)`），严禁使用带协议和系统用户名的绝对 URI；
- **严禁绑定特定 IDE 或私有环境路径**：文档中的构建与启动命令严禁写死个人 IDE 生成的特异性调试目录（如 `Desktop_Qt_...`）或私有环境绝对路径，必须统一使用跨平台通用的标准构建命令（如 `cmake -B build -S .`）与标准的 Python 原生独立虚拟环境 (`venv`)。

### 4. 【事实真实性检索与验证机制】
- 文档与设计中所依据的技术细节（动力学参数、接口定义、自由度限制、依赖版本），必须通过工具实时查询或实机实测确保真实，严禁臆测编造，严禁盲信历史陈旧缓存；
- 物理参数（如电机扭矩、转速、轮径、轴距、质心高度与接触摩擦系数）必须严格契合真实硬件物理规格，严禁设置脱离物理规律的超现实参数。

### 5. 【构建产物与二进制零容忍】
- 严格审查 `.gitignore`，严防任何 `build/`、`bin/`、可执行程序、动态库（`.so` / `.a` / `.dll`）与临时编译缓存被暂存或提交到 Git 仓库。

### 6. 【语言规范】
- 所有文档、代码注释、Docstring 与对话交流一律使用 **简体中文 (Simplified Chinese)**，专业术语保留行业标准英文全称与缩写对照。

---

## 二、软件工程与模块化架构准则 (Modular Architecture)

本工程严格遵循关注点分离 (SoC)、单向依赖原则与高内聚低耦合分层架构，拒绝单体大脚本混写：

```text
mobile_robot_station/
├── simulation/      # 物理动力学底座与传感器仿真 (MuJoCo 引擎接入、Raycasting 激光测距、GStreamer 视讯流)
├── core_math/       # 空间几何代数与李代数核心库 (SE(3)/SO(3) 切空间微扰、四元数变换)
├── slam/            # 3D 激光惯导紧耦合里程计 (点云体素滤波、点到面 ICP 配准、ikd-Tree 全局增量建图)
├── navigation/      # 空间几何切片与自主避障导航 (2D Costmap 欧氏距离膨胀、A* 全局寻路、DWA 局部避障)
├── msh/             # MSH (Mobile Station Host) 主控微服务与 JSON-RPC 通信总线
├── qt_client/       # 工业数字孪生上位机客户端 (C++ / Qt 6 QML，并列核心子系统)
├── scripts/         # 业务组装与入口控制台 (仅负责参数解析与调度，严禁堆砌底层算法实现)
└── tests/           # 核心算法与传感器自动化测试套件
```

### 1. 纯逻辑与副作用隔离 (SoC)
- 数学运算（`core_math/`）与算法解算（`slam/`, `navigation/`）严禁直接依赖具体视讯渲染窗口或底层硬件 IO；
- 算法模块输入输出必须采用纯数据契约（NumPy 数组、明确定义的 dataclass 或标准接口结构），确保算法完全具备独立可测试性与可迁移性。

### 2. 单向依赖原则 (The Dependency Rule)
- 依赖关系必须自顶向下单向流动：
  $$\text{交互呈现 (qt\_client)} \longrightarrow \text{主控协调 (msh/scripts)} \longrightarrow \text{算法解算 (slam/navigation)} \longleftarrow \text{物理底座 (simulation)}$$
- 严禁出现跨层反向依赖或循环导入。

### 3. 工程目录架构与文档一致性维护铁律 (Directory Tree Synchronization)
- 全项目必须保持工程实际物理目录、代码组织结构与 `README.md` 中的工程目录树 100% 动态同步；
- 凡有任何新增、删除、移动模块或重构目录结构的改动，必须在同一次提交中同步更新 `README.md` 的对应目录树与单行职责注释，严禁出现文档脱节或信息滞后。

---

## 三、用户专属核心技术底座适配准则 (Tech Stack Affinity)

系统在技术实现与依赖组织上，必须优先遵循以下经过工程实战验证的黄金底座：

| 技术维度 | 选型技术 | 核心要求与工程优势 |
| :--- | :--- | :--- |
| **算力与运行环境** | **Linux 原生 + 多核 CPU 并行** | 深度贴合 Linux 多核 CPU 算力架构，利用 NumPy / SciPy 向量化与多线程加速，彻底杜绝强绑定独立高端 GPU / CUDA。 |
| **交互与数字孪生** | **C++ / Qt 6 QML** | 支持 Cyber 赛博工业风格 HUD 控制台，具备原生 3D 视口渲染与低延迟视频流秒级解耦挂载能力。 |
| **物理动力学底座** | **MuJoCo** | CPU 高效并行，具备高精度连续刚体碰撞接触动力学与毫秒级光线投射 (Raycasting)。 |
| **视讯流媒体总线** | **GStreamer (RTP / UDP / H.264)** | 工业级低延迟多媒体流媒体管道，实现车载前视与全局监控双通道解耦广播推流。 |
| **运行依赖管理** | **Python 原生独立虚拟环境 (`venv`)** | 杜绝全局环境污染，统一通过根目录精确无冗余的 `requirements.txt` 规范交付。 |
| **跨进程控制总线** | **JSON-RPC 2.0 (TCP Socket)** | 自描述轻量远程过程调用契约，实现 Qt 前端与后台主控服务全双工通信。 |
| **空间数学内核** | **NumPy / core_math (SE3/SO3)** | 提供严格的刚体三维变换、李群李代数切空间微分与欧氏几何运算支持。 |

---

## 四、闭环自主导航流水线状态机规范

全自主导航任务必须遵循“感知-校验-规划-执行-复位”的确定性状态机闭环：

```text
       ┌───────────────┐
       │   1. ACQUIRE  │ ──> 3D 激光雷达扫描 / IMU 惯导测量 / 空间几何先验获取
       └───────┬───────┘
               ▼
       ┌───────────────┐
       │  2. VALIDATE  │ ──> 高程切片过滤 / 代价栅格欧氏距离膨胀 / 空间安全走廊校验
       └───────┬───────┘
               ▼
       ┌───────────────┐
       │   3. PLAN     │ ──> A* 全局避障寻路 / 航点等间距密集插值 / 走廊安全平滑
       └───────┬───────┘
               ▼
       ┌───────────────┐
       │  4. EXECUTE   │ ──> DWA 局部动态窗口推演 / 动力学限幅 / 差速轮速解算与物理步进
       └───────┬───────┘
               ▼
       ┌───────────────┐
       │   5. ARRIVE   │ ──> 终点容差判定 / 驻车制动刹车 / 态势回放与报表归档
       └───────────────┘
```

任何环节出现路径阻断或碰撞风险时，必须立即平稳降速并触发安全避障回退策略，严禁盲目冲撞。



