# mobile_robot_station 项目工程开发规范与规则 (Project Rules)

本文档定义了本项目在开发、重构、文档编写与代码提交全流程中必须严格遵守的工业级工程规范。

---

## 1. 文档与代码文本规范 (Text & Style)
1. **严禁使用 Emoji**：
   - 所有工程文档 (README.md, 架构说明)、源码文件、代码注释、终端输出日志中，严禁出现任何 Emoji 图标与表情符号；
   - 保持严谨、专业、克制的工业级表达风格。
2. **语言与注释要求**：
   - 对外文档、模块说明与代码注释一律使用**简体中文 (Simplified Chinese)**；
   - 关键算法步骤必须附带数学原理说明与物理单位标注 (如 SI 单位制: m, rad, s, rad/s)。

---

## 2. 软件工程与模块化规范 (Modular Architecture)
1. **高内聚低耦合**：
   - 严禁编写单一庞大的臃肿脚本，严格按功能分层拆分模块；
   - `src/simulation/`：仅负责物理引擎封装、机器人动力学驱动、传感器数据采集；
   - `src/core_math/`：仅负责空间几何变换、李群李代数、四元数与矩阵运算；
   - `src/slam/`：专注于点云配准 (ICP/NDT)、增量 kd 树、状态估计与地图拼接；
   - `src/navigation/`：专注于栅格代价地图、路径规划与轨迹控制；
   - `src/gui/`：专注于 Qt 6 QML 数字孪生上位机呈现；
   - `scripts/`：仅作为轻量级入口，负责参数解析、模块组装与生命周期调度，业务逻辑下沉至 `src/`。
2. **硬件与算法适配铁律 (强 CPU + 核显)**：
   - 彻底规避重型 CUDA 框架与大显存依赖；
   - 充分压榨多核 CPU 算力，使用多线程并行计算与 AVX 向量化指令；
   - 算法时间复杂度与内存占用必须可预测、实时可控。

---

## 3. 资产管理规范 (Asset Integrity)
1. **自包含与无软链接**：
   - 严禁在仓库中使用外部软链接 (Symlink)；
   - 所有 3D 几何网格 (STL/OBJ)、纹理材质、动力学与传感器配置，均必须放置在 `assets/` 对应的独立归档目录中，保证仓库克隆后即刻开箱即用。
2. **轻量与清洁**：
   - 杜绝冗余中间文件、临时测试模型与编译二进制上传；
   - 通过 `.gitignore` 严格审计与拦截本地缓存文件。
